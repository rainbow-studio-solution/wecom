<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <!-- title="Display in password characters" -->
    <t t-name="web.DisplayPasswordButton" owl="1">
        <button type="button" t-ref="button" t-attf-class="btn btn-sm btn-primary o_display_password_button {{ props.className || '' }}" t-on-click.stop="onClick">
            <i t-attf-class="fa fa-eye{{encryption_status ? '-slash':''}}"/>
        </button>
    </t>

</templates>
