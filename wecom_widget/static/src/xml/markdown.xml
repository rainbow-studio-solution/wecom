<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="WeComMarkdown">
        <div t-attf-class="#{widget.className}" t-if="widget.mode === 'readonly'"></div>
        <div t-attf-class="#{widget.className}" t-if="widget.mode === 'edit'">
            <nav class="navbar navbar-expand-lg navbar-light bg-dark text-light">
                <ul class="navbar-nav mr-auto">
                    <a class="nav-link">
                        <i class="fa fa-columns fa-2" aria-hidden="true"></i> 显示预览
                    </a>
                </ul>
            </nav>
            <textarea class="markdown-body"></textarea>
        </div>
    </t>
</templates>
